import { StyleSheet, Text, View, Image, ScrollView, TouchableOpacity, image } from 'react-native';

const ProfileScreen = ({ route }) => {

  return (
    <View>
      <Text>"Hatice"</Text>
      <Text>"I love stickers"</Text>
    </View>
  );
}

 export default ProfileScreen;